# dependabot configuration for neuroscope
# checks for dependency updates weekly

version: 2
updates:
  # python dependencies (pip)
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 10
    # batch all python updates into a single pr to reduce noise
    groups:
      python-dependencies:
        patterns:
          - "*"
    labels:
      - "dependencies"
      - "automated"
    commit-message:
      prefix: "deps"
    ignore:
      # avoid major version bumps on core ml libraries without manual review
      - dependency-name: "torch"
        update-types: ["version-update:semver-major"]
      - dependency-name: "torchvision"
        update-types: ["version-update:semver-major"]
      - dependency-name: "numpy"
        update-types: ["version-update:semver-major"]

  # github actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    # batch all github-actions updates into a single pr
    groups:
      github-actions:
        patterns:
          - "*"
    labels:
      - "ci"
      - "automated"
    commit-message:
      prefix: "ci"
