# documentation build dependencies (kept separate so readthedocs / ci can build
# the docs without installing the heavy runtime stack; deps are mocked in conf.py)
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0
myst-parser>=2.0.0
