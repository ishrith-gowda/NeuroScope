# api reference

auto-generated reference for the installable `neuroscope` package. heavy runtime
dependencies are mocked at build time (see `conf.py`); modules that cannot be
imported are skipped with a warning rather than failing the build.

```{eval-rst}
.. autosummary::
   :toctree: _autosummary
   :recursive:

   neuroscope.data
   neuroscope.models
   neuroscope.training
   neuroscope.evaluation
```
