% sashimi 2026 -- ext a (patchnce hybrid), 8pp + up to 2pp refs LNCS, double-blind.
% VERIFY the exact 2026 page limit on sashimiworkshop.org/submission.
% anonymized: no names, emails, institutions, grant ids, repo urls.
% framing: a MECHANISM STUDY (complementary failure modes of cycle vs
% patch-contrastive content preservation) with corrected metrics + downstream
% validation -- NOT a "we added a loss" paper. numerics marked TODO pending the
% corrected evaluation. do NOT headline the global-moment ssim.

\documentclass[runningheads]{llncs}

\usepackage{graphicx}
\usepackage{amsmath,amssymb,bm}
\usepackage{booktabs}
\usepackage[hidelinks]{hyperref}
\usepackage{xcolor}
\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}

\begin{document}

\title{Cycle-Consistent Patch-Contrastive Harmonization of\\
Multi-Site Glioma MRI: A Mechanism Study\\with Downstream Validation}

\titlerunning{Cycle-Consistent Patch-Contrastive MRI Harmonization}

\author{Anonymous}
\authorrunning{Anonymous (SASHIMI 2026 submission)}
\institute{Paper ID \#TODO. Author and institution details omitted for double-blind review.}

\maketitle

\begin{abstract}
Scanner- and protocol-induced intensity shift confounds multi-institutional MRI
studies, and unpaired image translation is an increasingly common remedy.
Two largely disjoint mechanisms preserve anatomical content during translation:
\emph{global, bidirectional} cycle-consistency, and \emph{local, feature-space}
patch-contrastive alignment (PatchNCE). We argue these mechanisms fail in
complementary ways under unpaired glioma data---the cycle constraint admits an
appearance-leaking, boundary-smoothing equilibrium, while one-sided PatchNCE
lacks a global appearance anchor---and we study their combination on a
self-attention 2.5D tri-planar backbone. We frame the work as a \emph{mechanism
study}: rather than asking ``does adding PatchNCE help,'' we ask under what
weighting and measured by which task the cycle/contrastive combination resolves
a failure mode that neither component resolves alone. On a
BraTS\,$\leftrightarrow$\,UPenn-GBM benchmark we evaluate with distribution
metrics (FID/KID, domain-classifier confusion), brain-masked \emph{windowed}
structural similarity, and \emph{downstream} cross-site glioma sub-region
segmentation (Dice/HD95), ablating the contrastive weight
$\lambda_{\mathrm{NCE}}\in\{0,0.1,0.5,1.0,2.0\}$ at a matched fine-tuning budget.
Across a matched-budget, two-seed sweep, a moderate contrastive weight robustly
improves over pure cycle-consistency: it lowers Fr\'echet distance to a broad
optimum ($\lambda_{\mathrm{NCE}}\!\in\![0.5,2.0]$) while the cycle-only control is
both the worst and the least stable arm, monotonically and significantly improves
brain-masked structural preservation ($0.27\!\to\!0.44$; paired $p{<}10^{-15}$),
and directly sharpens the tissue boundaries cycle-consistency over-smooths
($p{<}10^{-80}$). Yet under a corrected, training-consistent
protocol the cross-site segmentation gap is already small (cross-site Dice
$\approx$ within-site) and image harmonization does not close it, degrading a
real-image-trained segmenter uniformly across $\lambda_{\mathrm{NCE}}$. Distributional
harmonization quality and downstream task utility are therefore distinct axes.
\keywords{MRI harmonization \and unpaired translation \and contrastive learning \and cycle-consistency \and downstream validation.}
\end{abstract}

\section{Introduction}
\label{sec:intro}
Pooling magnetic resonance imaging (MRI) across institutions is essential for
statistically powered neuro-oncology studies, yet scanner vendor, field strength,
receive-coil configuration, and acquisition protocol induce systematic intensity
and contrast variation that confounds quantitative analysis. Feature-space
correction such as ComBat~\cite{fortin2017combat} removes batch effects from
\emph{extracted features} but cannot produce harmonized \emph{images}, and is
therefore unavailable to spatial downstream tasks---tumor segmentation, lesion
volumetry, voxel-level radiomics---that operate on the image itself. This
motivates generative, image-space harmonization, in which an unpaired translation
model maps an image acquired at one site to the appearance of another while
preserving anatomy.

The central difficulty is preserving content. Two mechanisms dominate the
unpaired-translation literature. \emph{Cycle-consistency}~\cite{zhu2017cyclegan}
enforces content preservation \emph{globally and bidirectionally} through
pixel-space reconstruction, but the cycle constraint is satisfiable by an
appearance-leaking equilibrium: under unpaired data with pathology, the generator
can hide source information in low-amplitude, high-frequency signal that survives
the round trip, manifesting as over-smoothed tissue boundaries and, in glioma
cohorts, attenuated or hallucinated lesion texture. \emph{Patch-contrastive}
translation (CUT/PatchNCE~\cite{park2020cut}) instead preserves content
\emph{locally in feature space} by maximizing patch-level mutual information,
which is robust to the cycle's failure mode---but applied one-sided it provides
no global anchor on the target appearance distribution and is known to require
aggressive weighting and careful negative sampling to remain
stable~\cite{wang2021negcut,zhan2022monce}. We hypothesize that these failure
modes are \emph{complementary}, and we study their combination on a
self-attention 2.5D tri-planar backbone for within-modality glioma harmonization.

A second, methodological point motivates this work. Harmonization quality is
routinely reported with image-similarity scores, but a global structural-similarity
index over an MRI slice is dominated by the large background region and is
misleadingly high; worse, it does not reveal whether disease-relevant signal is
preserved. The appropriate test is \emph{downstream}: does a model trained at one
site retain its accuracy when applied, after harmonization, to another? We
therefore center our evaluation on cross-site tumor-segmentation transfer, and we
report distribution-matching and \emph{brain-masked windowed} similarity rather
than a single global score.

\paragraph{Contributions.}
\begin{enumerate}
  \item A \emph{mechanism} analysis of cycle-consistency versus patch-contrastive
        content preservation for within-modality MRI harmonization, with a
        controlled $\lambda_{\mathrm{NCE}}$ ablation (including the
        $\lambda_{\mathrm{NCE}}{=}0$ cycle-only control) at a matched fine-tuning
        budget that locates the operating regime in which the combination is
        beneficial.
  \item A downstream-validated, training-consistent evaluation protocol on glioma
        cohorts (BraTS/TCGA-GBM, UPenn-GBM): cross-site sub-region segmentation
        Dice/HD95, distribution-matching (FID/KID, domain-classifier confusion),
        and brain-masked windowed similarity. We show that a naive
        harmonization-for-evaluation pipeline (mismatched channel order and
        normalization) silently corrupts results, and we describe the consistent
        protocol that avoids it.
  \item An honest, downstream-grounded finding. The patch-contrastive term
        improves distributional alignment and structural preservation with an
        interior optimum at $\lambda_{\mathrm{NCE}}{=}0.5$; yet, measured with the
        corrected protocol, image harmonization does \emph{not} improve cross-site
        glioma segmentation for this cohort pair---the raw cross-site gap is
        already negligible and a real-image-trained segmenter is sensitive to
        synthesized texture. We argue that distributional quality does not entail
        downstream benefit, and that downstream-grounded evaluation must be
        reported even when it disagrees with distribution metrics.
\end{enumerate}

\section{Related Work}
\label{sec:related}
\paragraph{Contrastive unpaired translation.}
CUT~\cite{park2020cut} replaced cycle-consistency with a one-sided multi-layer
PatchNCE objective drawing negatives from within the input image;
DCLGAN~\cite{han2021dclgan} showed contrastive and cycle-style dual objectives
are complementary on natural images. Subsequent work refined where and how
patches are sampled---query selection~\cite{hu2022qsattn}, semantic-relation
consistency~\cite{jung2022src}, hard-negative generation~\cite{wang2021negcut},
and modulated negatives~\cite{zhan2022monce}---all instances of the InfoNCE
estimator~\cite{oord2018cpc}. We deliberately \emph{re-introduce} full
cycle-consistency on top of multi-layer PatchNCE---the inverse of CUT's design
choice---and justify it rather than hide it: cycle supplies the global appearance
anchor that one-sided PatchNCE lacks, so the contrastive term can act as a
complement at a weight well below the CUT/FastCUT regime.

\paragraph{MRI harmonization.}
Beyond feature-space ComBat~\cite{fortin2017combat}, image-space harmonizers
include disentanglement methods (CALAMITI~\cite{zuo2021calamiti},
HACA3~\cite{zuo2023haca3}), the 2.5D VAE-GAN ImUnity~\cite{cackowski2023imunity},
and the many-to-one CycleGAN IGUANe~\cite{roca2024iguane}. The closest
methodological precedent, cycleCUT~\cite{wang2022cyclecut}, combines cycle and
contrastive losses---but for cross-modality MR$\to$CT synthesis, with no
within-modality harmonization and no lesion-level downstream task; one-sided
contrastive medical translation otherwise relies on auxiliary segmentation
supervision~\cite{seibel2024oct}. To our knowledge no prior work studies a
cycle\,$+$\,multi-layer-PatchNCE hybrid for within-modality MRI harmonization
with glioma downstream validation. We position our work as a mechanism study in
that gap rather than as a new loss.

\paragraph{Evaluation.}
We follow stronger conventions than image similarity alone: distribution matching
(FID/KID~\cite{heusel2017fid,binkowski2018kid}, domain-classifier confusion),
brain-masked \emph{windowed} similarity rather than a global moment, and downstream
segmentation~\cite{isensee2021nnunet}, all aggregated at the subject level.

\section{Method}
\label{sec:method}
\paragraph{Backbone.}
We build on a self-attention CycleGAN with a 2.5D tri-planar generator: each
input stacks three consecutive axial slices $(s_{z-1},s_z,s_{z+1})$ across four
modalities (FLAIR, T1, T1ce/T1gd, T2), giving inter-slice context at the memory
cost of a 2D model, with self-attention to capture the spatially non-stationary
component of scanner bias. The generators $G_{A\to B},G_{B\to A}$ output the
harmonized center slice; the architecture is summarized in
Fig.~\ref{fig:method}.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figs/method_diagram.pdf}
  \caption{Self-attention 2.5D CycleGAN with per-layer PatchNCE projection heads.
  Cycle-consistency provides a global, bidirectional appearance/anatomy anchor;
  multi-layer PatchNCE provides a local, feature-space content regularizer.}
  \label{fig:method}
\end{figure}

\paragraph{Cycle-consistency (global anchor).}
For unpaired collections $\mathcal{D}_A,\mathcal{D}_B$,
\begin{equation}
  \mathcal{L}_{\mathrm{cyc}} =
  \mathbb{E}_{x_a}\!\big[\|x_a-G_{B\to A}(G_{A\to B}(x_a))\|_1\big]
  +\mathbb{E}_{x_b}\!\big[\|x_b-G_{A\to B}(G_{B\to A}(x_b))\|_1\big].
  \label{eq:cyc}
\end{equation}

\paragraph{Multi-layer PatchNCE (local anchor).}
Encoder features at $L$ layers are passed through 2-layer MLP heads $h_\ell$ and
L2-normalized. For a query $z$, its positive $z^{+}$ at the corresponding spatial
location, and internal negatives $z^{-}$, the InfoNCE loss is
\begin{equation}
  \ell(z,z^{+},z^{-}) = -\log
  \frac{\exp(z\!\cdot\!z^{+}/\tau)}
       {\exp(z\!\cdot\!z^{+}/\tau)+\sum_{n}\exp(z\!\cdot\!z^{-}_{n}/\tau)},
  \quad
  \mathcal{L}_{\mathrm{NCE}}=\mathbb{E}\sum_{\ell=1}^{L}\sum_{s}\ell(\cdot),
  \label{eq:nce}
\end{equation}
with temperature $\tau{=}0.07$, $L{=}5$ encoder stages, and $256$ sampled
locations per layer, following CUT~\cite{park2020cut}.

\paragraph{Hybrid objective.}
PatchNCE \emph{augments}, rather than replaces, cycle-consistency:
\begin{equation}
  \mathcal{L} = \mathcal{L}_{\mathrm{adv}} + \mathcal{L}_{\mathrm{cyc}}
  + \mathcal{L}_{\mathrm{idt}} + \mathcal{L}_{\mathrm{ssim}}
  + \lambda_{\mathrm{NCE}}\,\mathcal{L}_{\mathrm{NCE}}.
  \label{eq:total}
\end{equation}
We sweep $\lambda_{\mathrm{NCE}}\in\{0,0.1,0.5,1.0,2.0\}$ at otherwise identical
hyperparameters, where $\lambda_{\mathrm{NCE}}{=}0$ is the cycle-only control.
A selected weight below the CUT/FastCUT regime ($1.0$/$10.0$) is principled
rather than incidental: because cycle-consistency already supplies a strong
content constraint, the contrastive term should complement it, not dominate;
over-weighting re-introduces the instability that PatchNCE exhibits when it must
carry content alone.

\section{Experimental Setup}
\label{sec:exp}
\paragraph{Data.}
Site~$A$ is BraTS/TCGA-GBM ($88$ subjects) and site~$B$ is UPenn-GBM ($566$
subjects), each with four co-registered modalities and expert tumor
segmentations at $1\,\mathrm{mm}$ isotropic, intensity-normalized to $[0,1]$. We
split \emph{by subject} $80/10/10$ and sample axial 2.5D triplets over the central
axial range; all metrics are aggregated to the subject level.
\paragraph{Training.}
Each $\lambda_{\mathrm{NCE}}$ arm is warm-started from a converged base
SA-CycleGAN and fine-tuned for a matched budget (Adam, $\beta_1{=}0.5$; mixed
precision; A100), so that any difference across arms reflects the contrastive
weight and not the optimization budget.
\paragraph{Metrics.}
Distribution matching: FID/KID (harmonized vs target, per modality), domain-
classifier accuracy toward chance, and feature-space MMD; structure: brain-masked
\emph{windowed} SSIM (a global moment is misleading here); downstream: a U-Net
trained on one site and applied cross-site, raw versus harmonized (whole-tumor and
sub-region Dice, HD95). \emph{Critically, the harmonization used for downstream
evaluation reuses the exact training pipeline} (channel order, $[0,1]$
normalization, slice-major 2.5D interleaving); a mismatched pipeline corrupts the
generator input and can manufacture a spurious downstream regression.
\paragraph{Baselines / ablation.}
The $\lambda_{\mathrm{NCE}}$ sweep includes the cycle-only control
($\lambda_{\mathrm{NCE}}{=}0$) on the same backbone, data, and budget, which is the
primary ablation isolating the patch-contrastive term; a one-sided CUT-only
baseline is left to future work.

\section{Results}
\label{sec:results}
\paragraph{Patch-contrastive alignment robustly improves over cycle-consistency.}
Table~\ref{tab:harm} reports the sweep against the base SA-CycleGAN;
Fig.~\ref{fig:sweep} shows per-arm variability over two training seeds. Four
findings hold. First, every fine-tuned arm improves on the base backbone, and the
cycle-only control ($\lambda_{\mathrm{NCE}}{=}0$) is the worst \emph{and least
stable} of them ($\mathrm{FID}_{A\to B}{=}54.8{\pm}3.0$ across seeds), whereas the
hybrid arms are tightly clustered ($\pm0.1$--$0.7$): the contrastive term does not
merely lower the mean, it stabilizes training. Second, adding PatchNCE reduces
$\mathrm{FID}_{A\to B}$ to a \emph{broad} optimum over
$\lambda_{\mathrm{NCE}}\!\in\![0.5,2.0]$ ($48.0{\pm}0.7$ at the best mean,
$\lambda{=}0.5$), with the unbiased KID and both translation directions agreeing
on the ordering; the precise weight within this region is not critical. Third,
brain-masked structural preservation between source and harmonized image rises
\emph{monotonically} with $\lambda_{\mathrm{NCE}}$ ($0.27\!\to\!0.44$), and the
hybrid's advantage over the cycle-only control is highly significant at the
subject level (paired Wilcoxon $p{<}10^{-15}$, $n{=}88$). Fourth, all hybrid arms
drive the domain classifier to chance, whereas the cycle-only control
\emph{overshoots} below chance, consistent with an appearance-leaking equilibrium
that the contrastive term regularizes. We adopt $\lambda_{\mathrm{NCE}}{=}0.5$
(best mean FID, a moderate weight below the CUT/FastCUT regime), noting that
$[0.5,2.0]$ is statistically equivalent on FID.

\paragraph{The contrastive term directly counteracts boundary over-smoothing.}
The mechanism we posit---cycle-consistency over-smoothing tissue boundaries,
PatchNCE sharpening them---makes a falsifiable prediction we test directly. On the
\emph{same} subjects ($n{=}515$ paired), the hybrid's harmonized volumes have
significantly higher brain-masked gradient magnitude than the cycle-only control's
($+4.4\%$, paired Wilcoxon $p{<}10^{-80}$)---i.e.\ sharper tissue boundaries---while
carrying \emph{lower} high-frequency Laplacian energy. Together these indicate the
hybrid produces sharper \emph{coherent} edges with \emph{less} high-frequency
noise, whereas the cycle-only equilibrium hides source signal in noise-like
high-frequency content: direct, quantitative support for the over-smoothing
hypothesis.

\begin{table}[t]
\centering
\caption{Harmonization metrics across the contrastive-weight sweep (single seed),
with the base SA-CycleGAN backbone for reference. Domain-classifier accuracy is
raw$\to$harmonized (chance $=0.5$); we report both FID directions and the unbiased
KID alongside the source$\leftrightarrow$harmonized masked windowed structural
similarity. FID-optimal arm in bold.}
\label{tab:harm}
\input{table_ext_a_harmonization}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figs/fig_ext_a_multiseed.pdf}
  \caption{Contrastive-weight sweep over two training seeds (mean$\pm$std). Left:
  $\mathrm{FID}_{A\to B}$---the cycle-only control ($\lambda{=}0$) is worst and by
  far the most variable, while the hybrid reaches a broad, tight optimum over
  $[0.5,2.0]$. Right: brain-masked structural preservation rises monotonically and
  stably with $\lambda_{\mathrm{NCE}}$.}
  \label{fig:sweep}
\end{figure}

\paragraph{Downstream: a small cross-site gap that harmonization does not close.}
We next ask the decisive question---does harmonization help a downstream task?
Table~\ref{tab:down} and Fig.~\ref{fig:down} report cross-site glioma
segmentation with a U-Net trained on one site and applied to the other, raw
versus harmonized. The first observation is that \emph{the cross-site gap is
already small}: a segmenter trained on site~$A$ scores $0.78$ foreground Dice on
raw site~$B$, essentially matching its within-site performance ($0.77$ on $A$),
and the reverse direction behaves similarly ($0.77$ cross-site versus $0.86$
within-site). There is therefore little domain gap for image harmonization to
recover. The second observation is that harmonization does \emph{not} recover it
and in fact lowers Dice in both directions ($-53\%$ for $A\!\to\!B$, $-37\%$ for
$B\!\to\!A$ at $\lambda_{\mathrm{NCE}}{=}0.5$) and worsens boundary localization
(HD95 rising $6\times$ and $3\times$; Table~\ref{tab:down}). The effect is systematic rather
than specific to one arm: the cycle-only control degrades by a comparable margin
($-42\%$ in both directions), so the loss is a property of image harmonization
under this protocol, not of the contrastive term.

\begin{table}[t]
\centering
\caption{Cross-site segmentation, raw versus harmonized (foreground Dice,
subject-level, $\lambda_{\mathrm{NCE}}{=}0.5$). Within-site scores bound the
achievable transfer. The already-small raw cross-site gap is not recovered by
harmonization.}
\label{tab:down}
\begin{tabular}{lccccc}
\toprule
direction & raw Dice & harm.\ Dice & $\Delta$ & raw HD95 & harm HD95 \\
\midrule
BraTS\,$\to$\,UPenn (train $A$, test $B$) & 0.776 & 0.363 & $-0.414$ & 5.1 & 31.4 \\
UPenn\,$\to$\,BraTS (train $B$, test $A$) & 0.765 & 0.479 & $-0.286$ & 4.1 & 12.1 \\
\midrule
within-site BraTS & \multicolumn{5}{c}{0.765} \\
within-site UPenn & \multicolumn{5}{c}{0.855} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.72\linewidth]{figs/fig_ext_a_downstream.pdf}
  \caption{Cross-site segmentation Dice, raw (gray) versus harmonized (green), in
  both transfer directions. The raw cross-site baseline is already close to the
  within-site ceiling, and harmonization reduces rather than improves it.}
  \label{fig:down}
\end{figure}

\paragraph{Why harmonization does not help here.}
Two factors explain the gap. (i) Both cohorts are glioma datasets with
co-registered four-modality protocols normalized to $[0,1]$, so the residual
\emph{segmentation-relevant} cross-site shift is small---image harmonization is
expected to pay off precisely where it is least needed here. (ii) The segmenter is
trained on real images and applied to GAN-synthesized images; a diagnostic
comparison confirms harmonized volumes are anatomically faithful and retain tumor
contrast, but they carry synthesis texture that is out-of-distribution for a
real-image segmenter and redistribute per-modality contrast (e.g.\ weakening the
FLAIR cue that delineates whole-tumor). This is a train/test shift introduced
\emph{by} harmonization, not a failure of anatomical preservation.

\section{Discussion and Limitations}
\label{sec:discuss}
\paragraph{The mechanism question.}
The evidence supports the complementarity hypothesis on three independent axes. A
moderate patch-contrastive term improves distributional alignment (a broad FID
optimum over $[0.5,2.0]$, robust across seeds) and local structural preservation
(monotone in $\lambda_{\mathrm{NCE}}$, paired $p{<}10^{-15}$); it \emph{directly}
sharpens the tissue boundaries cycle-consistency over-smooths (paired
$p{<}10^{-80}$); and it \emph{stabilizes} training, the cycle-only control being
both the worst and the most seed-variable arm. The cycle-only control's
below-chance domain classifier and its noise-like high-frequency content are the
appearance-leaking equilibrium made measurable, and the contrastive term
regularizes both.

\paragraph{Distributional quality $\ne$ downstream utility.}
Our central negative result is methodological rather than a defect of the model.
Harmonization that is excellent by FID, domain confusion, and masked structural
similarity does not translate into downstream benefit when (a) the cross-site task
gap is already small and (b) the downstream model is trained on real images and
tested on synthesized ones. Crucially, we verified that the corrected,
training-consistent harmonization pipeline (matched channel order, $[0,1]$
normalization, slice-major 2.5D interleaving) produces anatomically faithful
volumes---ruling out the mismatched-pipeline artifact that can manufacture a
spurious regression---and the degradation nonetheless persists and is uniform
across $\lambda_{\mathrm{NCE}}$. The honest reading is that harmonization-quality
metrics and task utility are distinct axes, and downstream evaluation must be
reported even, and especially, when it disagrees with distribution metrics.

\paragraph{Limitations.}
(i) Two fine-tuning seeds per arm bound seed variance and confirm the ordering,
but a larger seed budget would tighten the flat-optimum boundary. (ii) Two-site, single-disease (glioma) scope: the
small raw cross-site gap need not generalize to larger cross-scanner shifts where
harmonization is known to help, and testing that regime is the natural next
experiment. (iii) The downstream segmenter is trained only on real images; a
train-on-harmonized or harmonization-as-augmentation protocol would disentangle
the synthesis-texture penalty from any alignment benefit and is the primary
journal-version extension. (iv) A one-sided CUT-only baseline is omitted; the
cycle-only ($\lambda_{\mathrm{NCE}}{=}0$) control isolates the contrastive term
within our design but not against pure CUT.

\section{Conclusion}
\label{sec:conclusion}
We presented a mechanism study of cycle-consistent, patch-contrastive MRI
harmonization on multi-site glioma data. A controlled contrastive-weight sweep
shows that PatchNCE complements cycle-consistency---improving distributional
alignment with a broad, seed-robust optimum over $\lambda_{\mathrm{NCE}}\!\in\![0.5,2.0]$
and monotonically, significantly tightening structural preservation---without sacrificing cycle
consistency. Under a corrected, training-consistent downstream protocol, however,
the cross-site segmentation gap is already small and image harmonization does not
close it, uniformly across $\lambda_{\mathrm{NCE}}$. We conclude that
distributional harmonization quality does not by itself entail downstream task
utility, and we argue for downstream-grounded, training-consistent evaluation as
standard practice in image harmonization.

\bibliographystyle{splncs04}
\bibliography{refs}

\end{document}
